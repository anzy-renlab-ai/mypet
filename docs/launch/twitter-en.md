# Twitter / X — English thread

## Tweet 1 (the hook, attach the GIF)

```
your Claude Code subscription is bored

I built a fluffy desktop cat for macOS that eats it for you

hover 1 second → cat chomps one `claude -p` call → speech bubble with a tip,
a prompt to try, a 打油诗, or a news headline. then naps.

zero CPU when idle. MIT. ↓
```
Attach: `docs/screenshots/feed-demo.gif`

## Tweet 2

```
six rotating tip themes, weighted toward the Claude Code niche:
☕ tip  💡 prompt-to-try  📰 news  🤓 TIL  😆 joke  🥟 打油诗

you never get the same kind of thing twice in a row. click the bubble to copy
to clipboard. menubar keeps the last 10.
```

## Tweet 3

```
the whole thing is one Swift binary. it shells out to the local `claude` CLI
so you don't need a separate Anthropic key — your existing subscription
quota powers each feed.

no telemetry, no server, no login.
```

## Tweet 4 (the dev-bait)

```
~280 LOC SwiftUI view, ~1000 LOC total, 87 tests, GitHub Actions on macOS 13.

`swift build && swift run mypet` and you have a cat.
```

## Tweet 5 (CTA)

```
github.com/anzy-renlab-ai/mypet

if your Claude Code quota also sits idle, star + run it for a day, tell me
what theme weighting feels right.
```

## Notes
- Post during US tech-Twitter awake hours (Tue–Thu 9am–noon Pacific).
- Reply quickly to first ~5 quote-tweets — that's what pushes the algo.
- If a big account engages, ask if they want to record their own demo GIF.
